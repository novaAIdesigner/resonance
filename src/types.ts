export type Release = {
  title: string
  summary: string
  href: string
}

export type ValuePillar = {
  title: string
  subtitle: string
  cta: string
  href: string
}

export type Scenario = {
  name: string
  description: string
  endpoint: string
  productHref: string
}

export type Testimonial = {
  customer: string
  quote: string
}